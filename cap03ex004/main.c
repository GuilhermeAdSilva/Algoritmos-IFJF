#include <stdio.h>
#include <stdlib.h>

int main()
{
    int cavalos;
    printf("Informe a quantidade de cavalos: ");
    scanf("%d", &cavalos);
    printf("A quantidade de ferraduras necessarias sera %d", cavalos*4);
    return 0;
}
